%h2.title= t('.title')
= form_with model: resource, as: resource_name, url: password_path(resource_name), html: { method: :post }, local: true do |f|
  = render "layouts/error_messages", f: f
  = f.text_field_with_label :email, autofocus: true, autocomplete: 'email'
  .buttons
    = f.submit_primary t('.submit')
    = render 'devise/shared/links'
