%p
  %b School class:
  = @question.school_class&.name
%p
  %b Name:
  = @question.name
%p
  %b Lesson:
  = @question.lesson&.name

.buttons
  = link_to 'Edit', edit_question_path(@question), class: 'button is-primary'
  = link_to 'Back', questions_path, class: 'button'
