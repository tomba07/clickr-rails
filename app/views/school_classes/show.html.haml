%p#notice= notice

%p
  %b Name:
  = @school_class.name

.buttons
  = link_to 'Edit', edit_school_class_path(@school_class), class: 'button is-primary'
  = link_to 'Back', school_classes_path, class: 'button'
